<p><%= t(:welcome, :scope => 'devise.mailer.confirmation_instructions', :email => @email) %></p>

<p><%= t(:body, :scope => 'devise.mailer.confirmation_instructions' %></p>

<p><%= link_to t(:link_text, :scope => 'devise.mailer.confirmation_instructions'), confirmation_url(@resource, :confirmation_token => @token) %></p>
