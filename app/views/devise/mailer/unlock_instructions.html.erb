<p><%= t(:hello, :scope => 'devise.mailer.unlock_instructions', :email => @resource.email) %></p>

<p><%= t(:body, :scope => 'devise.mailer.unlock_instructions') %></p>

<p><%= t(:instructions, :scope => 'devise.mailer.unlock_instructions') %></p>

<p><%= link_to t(:link_text, :scope => 'devise.mailer.unlock_instructions'), unlock_url(@resource, :unlock_token => @token) %></p>
