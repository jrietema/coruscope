= comfy_form_for @group, as: :group, url: {action: :update}, type: :vertical do |form|
  = render form

- if request.xhr?
  = content_for_ajax :top do
    = render :partial => 'layouts/admin/cms/topnav'